{
  "name": "anavai",
  "version": "1.0.0",
  "private": true,
  "description": "Anavai static website",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "npm --prefix backend/worker run dev",
    "worker:deploy": "npm --prefix backend/worker run deploy",
    "worker:tail": "npm --prefix backend/worker run tail"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
